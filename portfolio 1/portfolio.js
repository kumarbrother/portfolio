function toggleMenu() {
    const navLinks = document.querySelector('.nav-links');
    navLinks.classList.toggle('show');
}

function toggleContacts() {
    const contactsDiv = document.getElementById('mycontacts');
    contactsDiv.classList.toggle('show');
}
